var generateEl = document.getElementById("generate");

generateEl.addEventListener('click' , function(){
  var selectPaDiEl = document.getElementById("selectPaDi").value;
  var pariDisp = selectPaDiEl.value
  var selectNumEl = document.getElementById("selectNum").value;
  var numPers = selectNumEl.value
  var numCons = Math.floor(Math.random() * (5 - 1) + 1);
  var result = numPers + numCons ;

    if(pariDisp == "pari")  
    function isEven(){}
    message



})
